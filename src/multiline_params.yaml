multiline:
  nodes:
    {
      type: string_array,
      default_value: [],
      description: "Names for each node.",
      read_only: true,
      validation: { not_empty<>: [], unique<>: [] },
    }
